{
  "name": "feishu-expense-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/bot.mjs",
    "check": "find src test -name '*.mjs' -exec node --check {} \\;",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "openai": "^7.20.0",
    "zod": "^4.6.5"
  }
}
